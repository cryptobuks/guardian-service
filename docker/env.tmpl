NODE_ENV=${NODE_ENV}
PORT=${PORT}
JWKS_ENDPOINT=${JWKS_ENDPOINT}
DB_HOST=${DB_HOST}
DB_NAME=${DB_NAME}
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}
DB_PORT=${DB_PORT}
MONGO_URI_PREFIX=${MONGO_URI_PREFIX}